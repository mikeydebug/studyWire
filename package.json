{
  "name": "studywire",
  "version": "1.0.0",
  "description": "StudyWire Fullstack Deployment",
  "scripts": {
    "install:all": "cd backend && npm install && cd ../frontend && npm install",
    "build": "cd frontend && npm run build",
    "start": "cd backend && node server.js"
  }
}
